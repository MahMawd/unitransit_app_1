import 'package:flutter/material.dart';

class VoyagesWidget extends StatelessWidget {
  VoyagesWidget({super.key});
  final Voya
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: ,
    )
  }
}